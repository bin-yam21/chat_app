# Backend origin the frontend talks to. No trailing slash, no /api/v1 suffix.
# Local dev (default if unset):
#   VITE_API_URL=http://localhost:3000
# Production (your Render backend URL):
#   VITE_API_URL=https://chat-app-api.onrender.com
VITE_API_URL=http://localhost:3000
